{
  "name": "faydai-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"mocha test --timeout 20000\"",
    "emulator": "firebase emulators:start --only firestore"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.0.0",
    "mocha": "^10.4.0"
  }
}
